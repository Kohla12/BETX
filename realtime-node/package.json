{
  "name": "betx-realtime",
  "version": "1.0.0",
  "description": "BetX realtime engine (Socket.io + Redis + sports data listeners)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "ioredis": "^5.4.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.3"
  }
}

